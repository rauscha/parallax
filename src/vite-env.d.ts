/// <reference types="vite/client" />

// Injected at build time by vite.config.ts (define) — the short git SHA (or
// "dev"), shown in the footer so a deployed build is identifiable at a glance.
declare const __BUILD_ID__: string;
