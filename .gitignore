# Node / Vite
node_modules/
dist/
.DS_Store
*.log
npm-debug.log*
.vite/

# Build outputs
dsp/build/
# NOTE: public/braids.wasm + public/braids.js are intentionally TRACKED (not
# ignored) so the GitHub Pages CI can ship them without an Emscripten toolchain.
# Re-run `npm run wasm` to rebuild them, then commit the updated binaries.

# Emscripten cache (if it ends up in-tree)
.emscripten_cache/
.emscripten_ports/
emsdk/

# Env / local
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
*.swp

# OS
Thumbs.db
desktop.ini

# Claude Code local settings (project-scoped)
# .claude/settings.local.json is kept committed for the overnight allowlist
.claude/scheduled_tasks.lock
.claude/cache/
