# Waiting on you

Updated 2026-05-31 (desktop). **Nothing blocking.** Open decisions now live in
`NEXT-STEPS.md` → "Open decisions"; this file just records what's been cleared.

## Resolved this session
- ✓ **Audio sanity-listen** — done on the desktop with headphones; sounds great across
  families. (Was the only thing the overnight run couldn't do.)
- ✓ **Model-dropdown cycling bug** — reported during testing, fixed (`0d312e0`).
- ✓ **Desktop pick-up** — this machine is set up; `public/braids.{wasm,js}` + deps are
  present via the GDrive folder sync (Emscripten only needed to rebuild WASM).

## Not yet done (carried forward — see NEXT-STEPS.md)
- Post-M1 **visual** fixes were only seen in passing, not explicitly verified. Eyeball
  during M2: Parallax branding, `audio ● READY`, theme chip `●`/`○`, flat idle scope line.

## Still open — your call (now tracked in NEXT-STEPS.md)
- **M2 interaction defaults** — vertical-drag knobs; chip + steppers + searchable
  family-grouped picker. Will build with these unless you say otherwise.
- **4-char model codes** for pictogram glyphs — purely cosmetic, rename freely.
